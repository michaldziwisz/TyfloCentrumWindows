<UserControl
    x:Class="Tyflocentrum.Windows.App.Views.InAppBrowserView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Loaded="OnLoaded"
    Unloaded="OnUnloaded"
    mc:Ignorable="d">
    <!-- a11y: needs manual verification — zachowanie czytników ekranu wewnątrz WebView2 zależy od zawartości strony i zainstalowanego runtime -->
    <Grid MaxWidth="1280" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="8">
            <TextBlock
                x:Name="ViewTitleTextBlock"
                AutomationProperties.HeadingLevel="Level2"
                Style="{ThemeResource TitleTextBlockStyle}"
                TextWrapping="WrapWholeWords" />
        </StackPanel>

        <StackPanel
            Grid.Row="1"
            Orientation="Horizontal"
            Spacing="8">
            <Button
                AutomationProperties.Name="Odśwież widok czytania artykułu"
                Click="OnRefreshClick"
                Content="Odśwież" />
            <Button
                AutomationProperties.Name="Otwórz artykuł w zewnętrznej przeglądarce"
                Click="OnOpenExternalClick"
                Content="Otwórz w przeglądarce" />
        </StackPanel>

        <Grid Grid.Row="2">
            <ProgressRing
                x:Name="LoadingIndicator"
                HorizontalAlignment="Left"
                IsActive="False"
                Visibility="Collapsed" />
            <InfoBar
                x:Name="ErrorBar"
                AutomationProperties.LiveSetting="Assertive"
                IsClosable="False"
                IsOpen="False"
                Severity="Error" />
        </Grid>

        <controls:WebView2
            x:Name="BrowserView"
            Grid.Row="3"
            AutomationProperties.AutomationId="InAppBrowser.WebView"
            AutomationProperties.Name="Widok czytania artykułu"
            NavigationCompleted="OnNavigationCompleted"
            NavigationStarting="OnNavigationStarting" />
    </Grid>
</UserControl>
