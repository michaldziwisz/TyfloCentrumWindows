<UserControl
    x:Class="TyfloCentrum.Windows.App.Views.TyfloSwiatPageDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Loaded="OnLoaded"
    mc:Ignorable="d">
    <Grid RowSpacing="12" MaxWidth="760">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock
                FontWeight="SemiBold"
                Text="Strona TyfloŚwiata" />
            <TextBlock
                AutomationProperties.HeadingLevel="Level2"
                Style="{ThemeResource TitleTextBlockStyle}"
                Text="{Binding Title}"
                TextWrapping="WrapWholeWords" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="{Binding PublishedDate}" />
        </StackPanel>

        <StackPanel
            Grid.Row="1"
            Orientation="Horizontal"
            Spacing="8">
            <Button
                AutomationProperties.Name="{Binding FavoriteButtonAutomationLabel}"
                Click="OnToggleFavoriteClick"
                Content="{Binding FavoriteButtonLabel}" />
            <Button
                AutomationProperties.Name="{Binding ShareButtonAutomationLabel}"
                Click="OnShareClick"
                Content="{Binding ShareButtonLabel}" />
            <Button
                AutomationProperties.Name="Otwórz stronę TyfloŚwiata w przeglądarce"
                Click="OnOpenInBrowserClick"
                Content="Otwórz w przeglądarce" />
            <Button
                Click="OnRetryClick"
                Content="Spróbuj ponownie" />
        </StackPanel>

        <Grid Grid.Row="2">
            <TextBlock
                x:Name="StatusTextBlock"
                AutomationProperties.LiveSetting="Polite"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                TextWrapping="WrapWholeWords"
                VerticalAlignment="Top"
                Visibility="Collapsed" />
            <ProgressRing
                x:Name="LoadingIndicator"
                HorizontalAlignment="Left"
                IsActive="False"
                Visibility="Collapsed" />
            <StackPanel
                x:Name="ErrorPanel"
                Margin="0,28,0,0"
                Visibility="Collapsed">
                <InfoBar
                    x:Name="ErrorBar"
                    IsClosable="False"
                    IsOpen="False"
                    Severity="Error" />
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="3">
            <TextBlock
                x:Name="ContentTextBlock"
                AutomationProperties.AutomationId="TyfloSwiat.Page.Content"
                Text="{Binding ContentText}"
                TextWrapping="WrapWholeWords"
                Visibility="Collapsed" />
        </ScrollViewer>
    </Grid>
</UserControl>
